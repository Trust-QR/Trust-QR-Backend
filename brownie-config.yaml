dotenv: .env
networks:
  default: development
  no_deploy: True
  sepolia:
    verify: True
  development:
    verify: False
  mainnet-fork:
    verify: False
  ganache-local:
    verify: False
    no_deploy: True

wallets:
  from_key: ${PRIVATE_KEY}
  dummy_key: ${DUMMY_PRIVATE_KEY}
